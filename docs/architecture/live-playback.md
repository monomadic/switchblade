# Live playback

These notes are the durable record of how the subsystem works **and of the bugs that shaped it** — the war stories are load-bearing, not history. Read the relevant section before changing this area.

## Live playback

The selected clip decodes **once** at quickview resolution (`quickview_max_*`, capped at source res) into the mipmapped hires texture — the tile samples it downscaled, quickview samples it big; one decoder, one timeline, so quickview opens instantly with no handoff/skip. Runs at animation level `normal`+ (quickview skips the settle delay). **Animation levels** (`animation` config / `--animation` CLI, default `normal`): `none` = snap-everything (every spring/fade completes in one frame), `minimal` = UI tweens only, `normal` = + live video. Gates: `level().ui()/live()`; `--animation full` is a legacy alias for normal (the old `full` grid sheet-cycling is removed; the `--no-anim` CLI flag was removed 2026-07-22). The GRID has no second lane — the one hires lane follows attention (hovered tile while mousing, selection while keyboard-navigating; DESIGN.md §15, adopted 2026-07-24). The tile-size hover lane survives for the quickview **filmstrip**, where a hovered chip plays in a never-evicted atlas slot. All lanes seek-match the thumbnail's frame (`thumb_seek_fraction`, default 10% of duration via cached meta) and die on target move / focus loss. **The start point is one config** (`thumb_seek_fraction`): it drives BOTH the static thumb extraction (`Recipe::seek_fraction`, encoded in the artifact name so a change regenerates — the default omits the `_sNN` suffix, any other value rebuilds) and the live seek (captured once at startup into `seek_fraction`, so the thumb and the first live frame always agree — the no-jolt handoff). Both are **keyframe grabs** at that fraction (`-noaccurate_seek` **+ `-copyts`** for the thumb; keyframe-start with no decode-forward for `SeekablePlayer`), NOT exact seeks: they land on the same keyframe ≤ the fraction, so the handoff still matches frame-for-frame while every thumb skips the per-clip GOP decode-forward (a big win for the gen sweep on sparse-keyframe 4K). **`-copyts` is load-bearing and easy to lose:** input `-ss` rebases timestamps so the landing keyframe comes out NEGATIVE, and the jpeg mux silently drops negative-pts frames — without `-copyts` the file written is the first pts≥0 frame, i.e. the EXACT target (decode-forward and all), up to a GOP ahead of where live opens. That shipped as "video skips backward on hover, variable length, every clip" and survived weeks of pts-based verification because showinfo/null-sink probes DO emit the keyframe — **verify seek behavior by comparing PIXELS of the written artifact (SSIM vs reference frames), never by pts logs.** The `SB_HANDOFF_DUMP=<dir>` env var dumps each lane's first served frames as pts-named PPMs + the cached thumb for exactly this kind of field diagnosis (the thumb path is passed as a **closure**, resolved only once the env var and the first-frame gates pass — as an eager argument it ran up to 6 source `stat`s per lane spawn with the dump switched off, i.e. 6 network round-trips producing nothing). **`SeekablePlayer` speaks content-relative time** (it adds the stream's `start_time`/edit-list offset when seeking and subtracts it when reporting position): the CLI `-ss` thumb seek is content-relative, so without this a file with a non-zero start_time (phone/camera/QuickTime footage) opened the live stream on a *different* keyframe than the thumb — the handoff jolt the whole no-jolt design exists to prevent (`start_time_offset_is_content_relative`). Changing one side to exact without the other reintroduces the jolt. 0.0 opens at the very beginning. Background ffmpeg/ffprobe jobs run `nice -n 10` — user attention wins CPU.

## One live player (`SeekablePlayer`)

Every live lane — selected stream, warm pool, hover — rides **`SeekablePlayer`** (`sb-media/src/seekable.rs`) — in-process libav via `rsmpeg` (DESIGN.md §15, settled + benchmarked 2026-07): resident demuxer + VT session per stream, so `seek(target, exact)` is a demuxer jump + decoder flush (keyframe ~10–30ms; exact = decode-forward, GOP-bound) instead of the CLI era's ~1s respawn, and cold spawns reach first frame ~2× sooner. It replaced an earlier ffmpeg-CLI-pipe player (`LivePlayer`, now **removed** 2026-07-22 — the in-process path soaked a release; git history has it if the revert is ever needed). Contract: frames due-stamped into a small bounded queue (depth 3 = `LIVE_QUEUE_DEPTH` — decode-ahead absorbs keyframe/cold-start spikes), `take_frame` surfaces a frame only once due, so presentation paces on the **render clock**; late frames **re-anchor** instead of accruing debt (debt = stutter-then-fast-forward). Stamps by **pts delta** from a wall-clock anchor (degrades wrong-meta/VFR to the right wall-clock speed), and the whole player speaks **content-relative time** (start_time offset applied on seek, subtracted on position — see the Live playback bullet). Decode gates are meta-driven: VT on macOS **only for h264/hevc/prores** (VP9/AV1 benchmarked *slower* through VT); **hardware scaling** ([perf review 01](docs/perf-reviews/01-live-video-pipeline.md) — cut 4K60→1440p from ~3.7 to ~0.9 cores) for VT codecs with known 8/10-bit 4:2:0 `pix_fmt` via `scale_vt`+`hwdownload` at target res, dims rounded DOWN to mod-8, ±90° via `transpose_vt` (PSNR-verified) — `SeekablePlayer` reuses the exact `hw_scale_vf` string (shared with the ffmpeg thumbnail chain) as its libavfilter graph, so the chains can't drift; anything else takes the sw chain (which `SeekablePlayer` rotates explicitly — libavfilter doesn't autorotate like the CLI; 90/180/270 covered). Missing `pix_fmt` (old cache) → sw chain now + queued reprobe heals next spawn. The bounded queue is what makes pre-warm free: an undrained player stalls its decoder via backpressure after ~4 frames. Drop MUST wake the condvar (`closed` + notify): a stalled player's reader parks on the full-queue wait where nothing else can reach it — without the wake every dropped warm player leaked a thread pinning ~30MB (live video degraded after minutes of browsing). Warm spawn order is a priority (serialized one at a time): right, right+1, down, left — no 'up' (rare); the second-right slot makes repeated right-taps instant. **Continuity handoff (`hover_resume`):** a selected-lane open (warm promotion or fresh spawn) for a clip whose hover preview is/was just playing continues from the PREVIEW's position — keyframe seek in place on promotion, spawn-at-position otherwise — never the thumb anchor: the warm stream spawns at the anchor and sits parked while the preview plays ahead, so promoting its queued frames jumped playback backward by the whole preview duration (~6s measured on a 4s-GOP 4K clip — the "video skips backward on click" bug; the position is tracked while the lane lives and survives its death for as long as the selection stays on that clip, since the open can trail the click by the settle delay; cleared by shuffle/D-swap like every index-keyed thing). Tests: `hover_click_continues_from_the_preview_position` (sb-app), scenario `benchmarks/scenarios/hover_then_select_handoff.toml`. Regression tests (all `seekable::tests`): `seekable_player_paces_frames`, `unwatched_seekable_player_stalls_then_serves_instantly`, `dropped_seekable_player_releases_its_reader`, `seek_jumps_in_place_without_respawn`, `start_time_offset_is_content_relative`.

## Lane spawns never read the disk (async cached meta, 2026-07-24)

Every lane spawn needs the clip's cached `meta.json` — duration picks the seek anchor, coded dims + rotation pick the decode size, `pix_fmt` gates the hardware chain. That read used to happen **inline in `frame()`** (`clip_meta`, behind a session memo): a `stat` of the source plus a small read of the local cache entry. On a local SSD it is microseconds; over SMB it inherits the contended link's tail — **401 ms mean, 901 ms worst, and one 902.7 ms `frame()` that was *entirely* that single call** ([perf review 05 §3](../perf-reviews/05-zoom-storm-scheduler.md); a zoom-out reproduces it reliably by flooding tier 1 and saturating the link). Two changes, both load-bearing:

- **The source `stat` is gone, not moved.** The ingest thread already stats every file (it carries `created` for sorted ingest); it now also carries `fingerprint_key` = `(size, mtime_secs)` on `Ingested` → `Clip.fp`, and `sb_media::cached_meta_keyed` resolves the cache entry from those. What remains is a read of the **local** cache volume — the clip's own disk is never touched. (`fp` is `None` for demo tiles and cloud placeholders; then `MetaService` stats for itself, off-thread.)
- **The remaining read runs on `MetaService`** — one dedicated thread, deliberately **not** the ffmpeg worker pool, whose three workers sit in ~1 s jobs on a network volume; a meta read queued behind them would land far too late to anchor a spawn. `clip_meta` is now pure memory: a memo hit answers, a miss queues the read and answers `MetaLookup::Pending`, and the spawn path **returns no lane that frame** and retries on the next one (the service's notify wakes the deadline-paced loop). One frame of delay buys the right anchor and decode size — the alternative, spawning on fallbacks, opens the clip at 0:00 and then jolts, which is the exact bug `thumb_seek_fraction` exists to prevent.

Three rules to keep: **resolve the meta BEFORE claiming an atlas slot** (a deferred spawn must not hold a slot for the frames it waits); **`Pending` must be able to end** — `meta_cache` stores `Option<Meta>`, so "this clip has no cached meta" is a real memoized answer, and `Tuning::meta_wait_ms` (default 120 ms) caps the wait so a wedged lookup can never hold playback back (counted as `meta_wait_expired`; fluidity beats exactness, hard rule); and a **static thumb arriving is what makes a clip spawn-eligible**, so `drain_media` prefetches its meta right there — by the time the pointer or selection reaches the tile the lookup is a memo hit and nothing defers at all. Incomplete metas (pre-`pix_fmt` entries) are memoized like any other, and `heal_meta` **drops the stale entry from the memo** at the spawn that noticed, so the next spawn re-reads and adopts whatever the reprobe wrote — the old "re-read from disk every spawn" without the disk. Invalidating from `clip_meta` instead would look tidier and is a trap: it is also called per frame (`resolve_selected_aspect`), so a stale entry would queue a lookup — and a wake — every frame. Measured on `hover_then_select_handoff`: **20 render-thread fs calls → 1**, `meta 0×`, `meta_wait_expired=0`. Tests: `clip_meta_memoizes_and_heal_meta_queues_the_src_heal`, `a_meta_lookup_that_never_lands_still_lets_the_lane_open`.

## Neighbor decoder pre-warm (the warm pool)

Neighbor decoders pre-warm for the four movement destinations (right, right+1, down, left) in quickview AND in the grid (when live video is on): `warm` holds parked-by-backpressure players, the outgoing stream demotes to warm instead of dying, promotion serves a queued frame the same tick — so single-step selection moves show video instantly. Two ordering rules, both load-bearing: promotion MUST run before the pool is pruned to the new destinations (the selection is never its own neighbor; pruning first killed the warmed decoder and made every advance pay full cold-spawn latency, ~1s on 4K — a cold spawn's floor is probe+VideoToolbox init+first GOP, benchmarked unfixable via flags); and fresh warm spawns wait for settle + the selected stream's first frame, then run one at a time (user attention owns the CPU).

## Focus pause

Losing window focus **parks** the selected stream and the warm pool (undrained → bounded backpressure stalls the decoders within a few frames; stream objects and timelines survive), drops the hover lane, and lets the idle throttle engage; refocus resumes the SAME decoders in place — late frames re-anchor, so playback continues from where it stopped (the old reap-on-defocus made every refocus pay a cold respawn that visibly jumped back to the thumb frame). No spawns of any kind while paused. `pause_unfocused` config (default true), `p` toggles at runtime. Test: `focus_pause_parks_and_resumes_the_same_stream`.

## Skip (`[` / `]`)

An in-place `player.seek(target, exact=false)` on the resident decoder — the stream object, its timeline, and `first_frame` all survive; no respawn, no checkpoint machinery (the old `warm_skip`/`skip_delta` pre-warm dance is deleted). The new offset wraps mod duration; `SelLive::position()` is the decoder's real pts (the seek target while one is in flight — the bar shows where playback is *going*). `hires_shown` (path of the frame in the hires texture) keeps the last frame on screen only through the keyframe jump+flush (~10–30ms) — near-instant, no freeze. **Skips are keyframe seeks** (changed 2026-07-21 from exact): the landing keyframe shows immediately instead of decode-forwarding across the GOP (~500ms on sparse-keyframe 4K — the mpv-style feel the user asked for); the small drift off the exact fraction is invisible for a jump. Same for chapter click / chapter step (`chapter_seek`/`chapter_step` pass `exact=false`). The seekbar scrub stays two-phase: keyframe while dragging + **exact** on release (a precise landing where you let go). Amount = `skip_fraction` tuning, or per-binding `amount` on an internal command. A slim position bar flashes over the video (selected tile or quickview modal) for `SKIP_BAR_HOLD_S` then fades. Test: `skip_seeks_the_selected_stream_in_place` (fixture is `-g 30` so keyframe seeks have a keyframe to land on). The runtime `SeekablePlayer::seek(_, exact)` API still supports exact for the scrub-release path.
---

Back to the [architecture index](README.md) · [CLAUDE.md](../../CLAUDE.md) · [DESIGN.md](../../DESIGN.md)
