[package]
name = "sb-app"
version = "0.1.0"
edition = "2024"

[dependencies]
sb-window = { path = "../sb-window" }
sb-media = { path = "../sb-media" }
serde = { version = "1", features = ["derive"] }
toml = "1"
log = { workspace = true }
env_logger = "0.11"
